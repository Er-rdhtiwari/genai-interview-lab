# Dockerfile.ui - Streamlit UI image
FROM python:3.11-slim AS base

ENV PYTHONDONTWRITEBYTECODE=1 \
    PYTHONUNBUFFERED=1

WORKDIR /app

RUN apt-get update && apt-get install -y --no-install-recommends \
    && rm -rf /var/lib/apt/lists/*

COPY pyproject.toml ./
COPY src ./src

RUN pip install --no-cache-dir .

# Streamlit default port
EXPOSE 8501

# BACKEND_URL controls which FastAPI backend to call.
# We leave default here; override per environment.
ENV BACKEND_URL="http://localhost:8000"

CMD ["streamlit", "run", "src/ui/revision_ui.py", "--server.port=8501", "--server.address=0.0.0.0"]
